{
  "name": "pokevolume",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ingest:ebay": "tsx jobs/ingestEbaySoldListings.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "prisma:generate": "prisma generate --schema db/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.3"
  }
}
